import { ScrollView, StatusBar } from "react-native";

function Mine() {
  return (
    <ScrollView>
      .
      <StatusBar barStyle="light-content" />
    </ScrollView>
  );
}

export default Mine;
